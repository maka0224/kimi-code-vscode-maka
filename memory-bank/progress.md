# Progress

## 版本与上游对应

| 本分支 | 基于上游 | 状态 |
| --- | --- | --- |
| 0.3.2 | 0.7.5 | 已提交代码，待实际验证输入建议交互、打包发布 |
| 0.3.1 | 0.7.5 | 已提交代码，待手动验证横幅交互、打包发布 |
| 0.3.0 | 0.7.5 | 已发布（提示词优化、上游 0.7.4/0.7.5 同步） |
| 0.2.3 | 0.7.3 | 已发布（桥接超时误判、错误码透传修复） |
| 0.2.2 | 0.7.3 | 已发布（会话命名、计划审阅优化） |
| 0.2.0 / 0.2.1 | 0.7.3 | 已发布 |
| 0.1.0 | 0.7.3 | 首个独立版本 |

## 已可用（本分支特性）

全中文界面、maka.* 命名空间、上下文用量统计、额度面板（5 小时进度条）、拖拽 `@` 引用、Alt+Tab 聚焦（含切回恢复修复）、斜杠补全、会话状态通知（6 个 maka.notifications.* 开关）、会话时长显示、对话过程折叠、蓝紫主题/新 Logo、计划审阅 Markdown 渲染、会话自动命名（默认关闭）、提示词优化（思考模式默认关闭）、引擎驱动 `@` 建议（高亮/文件夹引用）、陌生目录信任确认（webview 横幅，对齐 kimi-cli）、输入后续文字建议（ghost text，历史/大模型/混合三种方式，设置弹窗配置，默认关闭）

## 待办 / 下一步

- 0.3.1/0.3.2 手动验证（未信任目录横幅、Alt+Tab 焦点恢复、输入建议交互）并打包发布
- 跟踪上游 0.7.6+（同步方法见 systemPatterns.md）

## 已知问题

- `fileHistory` 的 2 个测试在 Windows 下因路径分隔符失败（上游固有，实验特性默认关闭）
- `package:verify` 脚本期望上游平台 vsix 命名，对通用包失配
- Windows 全量测试基线约 640 个环境性失败（fsync EPERM / symlink 权限 / 超时），评估改动需与基线对比
- `agentsMdReminder` 测试的 symlink 用例需管理员/开发者模式
- 取消目录信任暂无界面入口，需手动删除 `~/.kimi-code/workspace-trust/` 下对应文件

## 决策演进

- 0.3.0 版本号：上游同步本可发 0.4.0，按用户决定并入 0.3.0（0.3.0 当时未推送）
- 0.3.1 信任提示：用户拍板 webview 横幅（非 VS Code 模态框）、所有陌生目录都提示、「忽略」= 仅本次关闭下次仍提示
- 0.3.2 输入建议：内容来源用户选定「历史优先、LLM 兜底」混合模式 + Tab 接受；配置先落 VS Code 设置，用户拍板改为设置弹窗 prefs（VS Code schema 静态无法动态模型下拉）；默认关闭（避免未预期消耗额度）；建议模型下拉平铺去掉「Kimi Code Maka」服务商分组标题
- transcript 包：曾随 CLI 一起裁剪，0.3.0 同步时因 migration-legacy 测试需要按上游原文恢复
- ahooks：上游 0.7.5 全面移除，本分支跟随迁移到 TanStack Query
